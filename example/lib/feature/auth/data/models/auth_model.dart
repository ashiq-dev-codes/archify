import 'package:example/feature/auth/domain/entities/auth_entity.dart';

/// The data layer's shape of AuthEntity — add
/// fromJson/toJson (or your serialization format) here, then map to/from
/// AuthEntity.
class AuthModel extends AuthEntity {
  const AuthModel();

  // Add your fromJson/toJson (or similar) here
}
