abstract class AuthRepository {
  // Add your repository methods here, returning Result<T> from
  // core/error/failures.dart so callers can handle failure without
  // try/catch, e.g.:
  // Future<Result<AuthEntity>> getAuth();
}
